
using UnityEngine;
[System.Serializable]
public enum RemoteConfigName
{
    // ----- không được sửa xóa
    Force_update_to_new_version,
    Related_Apps,
    open_ad_config,
    ads_network_config,
    inter_ad_config,
    //---------------------------------
    rate_config,
    loading_config,
    offline_play_on_off,
    rank_play_config,
    remove_options_popup_complete,
    daily_gift,
    banner_id,
    interstitial_id,
    rewarded_id, 
    ads_open_id,
    reward_ad_config,
    moves_config,
    buttons_config,
    replay_config,
    out_of_moves_config,
    banner_id2
}
